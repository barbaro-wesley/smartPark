
# Sobrescreve o docker-compose.yml para uso com Arduino físico.
# Uso: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d

services:
  api:
    environment:
      - DEMO_MODE=false
    devices:
      - /dev/ttyACM0:/dev/ttyACM0
